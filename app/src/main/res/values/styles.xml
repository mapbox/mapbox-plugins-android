<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Translucent" parent="Theme.AppCompat.Light.NoActionBar"/>

    <style name="CustomLocationLayer" parent="LocationLayer">
        <item name="backgroundDrawable">@drawable/custom_user_stroke_icon</item>
        <item name="foregroundDrawable">@drawable/custom_user_icon</item>

        <item name="bearingDrawable">@drawable/custom_user_arrow</item>
        <item name="navigationDrawable">@drawable/custom_user_puck_icon</item>

        <item name="accuracyAlpha">0.15</item>
        <item name="accuracyColor">#FF82C6</item>
    </style>


</resources>
